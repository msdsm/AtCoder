#include <stdio.h>
#include <stdbool.h>
typedef long long ll;
#define rep(i,a,N) for(ll i=(a);i<(N),i++)

int main(void){
  ll a,b;
  scanf("%lld %lld",&a,&b);
  printf("%lld %lld",a,b);
  return 0;
}
